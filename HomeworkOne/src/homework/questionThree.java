package homework;

public class questionThree {

	public static void main(String[] args) {

		//Write a Java program that accepts two integers from the user and then prints the sum, the
		//difference, the product, the average, the distance (the difference between integer), the
		//maximum (the larger of the two integers), the minimum (smaller of the two integers)
		
		
		int x=20;
		int y=10;
		int z;
		
		int sum;
		int sub;
		int mul;
		int div;
		int avg;
		
		
		sum= x+y ;
		sub= x-y ;
		mul= x*y ;
		div= x/y ;
		avg= (sum/2);	
		
		System.out.println(sum);
		System.out.println(sub);
		System.out.println(mul);
		System.out.println(div);
		System.out.println(avg);
	
	}

}
